import{_ as t,o as a,c as o,a6 as n}from"./chunks/framework.ZMCvXf1_.js";const m=JSON.parse('{"title":"Why noorm","description":"noorm is the fifth attempt at one problem. Here is the problem, the CLIs that came before, and the questions that kept making the tool bigger.","frontmatter":{"title":"Why noorm","description":"noorm is the fifth attempt at one problem. Here is the problem, the CLIs that came before, and the questions that kept making the tool bigger."},"headers":[],"relativePath":"why-noorm.md","filePath":"why-noorm.md"}'),r={name:"why-noorm.md"};function s(i,e,h,d,l,c){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="why-noorm" tabindex="-1">Why noorm <a class="header-anchor" href="#why-noorm" aria-label="Permalink to “Why noorm”">​</a></h1><p>I stopped using ORMs and needed something to run the SQL.</p><p>That is the whole origin. Everything else in noorm arrived as an answer to a question I hit while working without one.</p><h2 id="the-tools-that-came-first" tabindex="-1">The tools that came first <a class="header-anchor" href="#the-tools-that-came-first" aria-label="Permalink to “The tools that came first”">​</a></h2><p>noorm is the fifth attempt. The first three were about the interface, and each one replaced the last because I needed something more robust: <strong>minimist</strong> first, then <strong>cmd-ts</strong>, then <strong>oclif</strong>. The bar kept rising. I wanted a CLI that was discoverable by someone being onboarded, easy to use without a manual, and easy to keep building on.</p><p>Then I scrapped all of it and moved to <strong>citty</strong> and <strong>@clack/prompts</strong>, which fit what noorm actually does far better than any of the three.</p><p>Around the same time I was experimenting with terminal interfaces and tried <strong>Ink</strong>. Testing it against the CLI settled something. Running commands means remembering commands. A screen that shows you your configs, your pending changes, and your schema while you decide is a better workflow than typing a verb and hoping you got the flags right, because the decision needs data in front of it and a CLI gives you none.</p><p>That produced the split noorm still has. Anything interactive lives in the <a href="/tui.html">TUI</a>: setup wizards, config editing, secrets, browsing the schema. Anything automatable lives in the <a href="/headless.html">CLI</a>, where it belongs, so the same operations run headless in CI with JSON output.</p><h2 id="the-questions-that-kept-arriving" tabindex="-1">The questions that kept arriving <a class="header-anchor" href="#the-questions-that-kept-arriving" aria-label="Permalink to “The questions that kept arriving”">​</a></h2><p>None of this was designed up front. It was excavated. Each row is a question I could not answer with what I had at the time.</p><table tabindex="0"><thead><tr><th>Question</th><th>What it became</th></tr></thead><tbody><tr><td>How do I run SQL?</td><td>A directory of files, executed in order</td></tr><tr><td>How do I undo one?</td><td>Forward and revert pairs, tracked per change</td></tr><tr><td>How do I test against it?</td><td>A real database with safety guards, not mocks</td></tr><tr><td>How do I express what DDL makes painful?</td><td>Templates that render SQL from YAML</td></tr><tr><td>How do I seed data?</td><td>Templates again, plus transfer between databases</td></tr><tr><td>Where does connection configuration live?</td><td>Configs you can export and hand to a teammate</td></tr><tr><td>How do I onboard someone onto four environments?</td><td>Stages, with required and optional variables</td></tr><tr><td>Where do team secrets live?</td><td>An encrypted vault table inside the database</td></tr><tr><td>What ran, by whom, and why did it break?</td><td>History that records the operator and the error</td></tr><tr><td>How do I re-run the idempotent objects?</td><td>Manifests</td></tr></tbody></table><p>Three of those are worth more than a table row.</p><h3 id="templates-are-not-just-per-environment-strings" tabindex="-1">Templates are not just per-environment strings <a class="header-anchor" href="#templates-are-not-just-per-environment-strings" aria-label="Permalink to “Templates are not just per-environment strings”">​</a></h3><p>Rendering one file differently for dev and production is the obvious use. It was not the reason.</p><p>Templates exist so seed data can be expressed and iterated on, and so YAML can become DDL that is miserable to write by hand. SQL Server&#39;s scheduled jobs are the clearest case: expressing a schedule semantically and rendering it into the DDL that creates the job beats hand-writing that DDL every time. The same goes for roles and permissions across stored procedures and views. A YAML list of who gets what is readable and diffable. The forty <code>GRANT</code> statements it renders into are neither.</p><p>So the templating layer is where iteration happens, and SQL is the output rather than the source you maintain.</p><h3 id="history-records-why-not-just-what" tabindex="-1">History records why, not just what <a class="header-anchor" href="#history-records-why-not-just-what" aria-label="Permalink to “History records why, not just what”">​</a></h3><p>Every applied change is recorded with its status, when it ran, <strong>who ran it</strong>, and the <strong>error message</strong> if it failed. That last one is the part even ORMs skip.</p><p>It matters when a change fails, you fix it, and you run it again. Something that broke during the failed attempt may not surface for days. A history with the actual error lets you trace back to which run went wrong instead of guessing. Recording the operator means a pattern of failures has a name attached to it.</p><p>The reason for a change almost never gets written down anywhere. That is what <code>changelog.md</code> in the change folder is for: the justification, next to the SQL it justifies.</p><p><strong>Manifests</strong> handle the objects that are meant to be re-run. Views, functions, stored procedures, and the templates that generate crons or permissions are all idempotent, so a manifest lists them and the change re-runs them. The point is that fixing a stored procedure stops being something that happens in a SQL editor at 11pm and starts being a tracked change like any other.</p><h3 id="configuration-is-something-you-hand-to-someone" tabindex="-1">Configuration is something you hand to someone <a class="header-anchor" href="#configuration-is-something-you-hand-to-someone" aria-label="Permalink to “Configuration is something you hand to someone”">​</a></h3><p>A config is exportable. Sharing one goes through the TUI, where export encrypts to the recipient&#39;s public key with X25519 key exchange and AES-256-GCM, and leaves credentials out, so the recipient supplies their own username and password on import. You can export local dev, local test, staging, and production, send all four, and the other person fills in only the parts that are theirs. (The CLI&#39;s <code>config export</code> is a different tool: plain JSON, credentials included, for your own backups.)</p><p><strong>Stages</strong> make that prescriptive. Rather than describing your environments in a README, you define them once, and everyone gets the same shape with the details left blank. A stage declares its variables as required or optional, and a build fails with a clear error when a required one is missing instead of rendering something half-formed. Configs are typed <code>local</code> or <code>remote</code>, which is the distinction that decides how careful the tool should be. Losing a local dev database costs nothing. Production is a different question, which is what <a href="/guide/environments/configs.html#access-roles">access roles</a> gate.</p><p><strong>The vault</strong> covers what stages cannot: secrets the whole team needs. Those live encrypted in the database itself, in <code>noorm.vault</code> where the dialect has schemas and a <code>__noorm_vault__</code> table where it does not, so they distribute automatically rather than over Slack. They sit lowest in the precedence chain, so a local override always wins.</p><h2 id="the-arrival-of-llms" tabindex="-1">The Arrival of LLMs <a class="header-anchor" href="#the-arrival-of-llms" aria-label="Permalink to “The Arrival of LLMs”">​</a></h2><p>The constraint changed. Writing SQL stopped being the slow part, so the slow part became reviewing SQL that an agent wrote quickly and confidently and sometimes wrongly.</p><p>That pushed the tool in three directions at once.</p><p><strong>Safeguards</strong>, so a confident mistake cannot reach production: per-channel <a href="/guide/environments/configs.html#access-roles">access roles</a>, dry runs, checksums, and a typed confirmation on anything destructive.</p><p><strong>Guidance</strong>, so an agent writes code that matches the conventions here instead of guessing at them. That is what the <a href="/getting-started/installation.html">agent skill</a> is for.</p><p><strong>Planning</strong>, because the cheapest moment to catch a bad schema is before it exists.</p><p>That last one turned into a separate tool. See <a href="/modeling/">Information modeling</a>.</p><h2 id="where-to-start" tabindex="-1">Where to start <a class="header-anchor" href="#where-to-start" aria-label="Permalink to “Where to start”">​</a></h2><div class="next-steps"><p><a href="/getting-started/installation.html"><strong>Installation</strong></a> Get noorm installed and running.</p><p><a href="/getting-started/concepts.html"><strong>Concepts</strong></a> How SQL files and changes divide the work.</p><p><a href="/modeling/"><strong>Information modeling</strong></a> Design the data model before you write the schema.</p></div>',34)])])}const g=t(r,[["render",s]]);export{m as __pageData,g as default};
