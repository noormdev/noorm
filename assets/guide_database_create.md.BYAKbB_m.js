import{_ as o,o as t,c as a,a6 as n}from"./chunks/framework.ZMCvXf1_.js";const f=JSON.parse('{"title":"Creating a database","description":"","frontmatter":{},"headers":[],"relativePath":"guide/database/create.md","filePath":"guide/database/create.md"}'),d={name:"guide/database/create.md"};function c(r,e,i,s,h,l){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="creating-a-database" tabindex="-1">Creating a database <a class="header-anchor" href="#creating-a-database" aria-label="Permalink to “Creating a database”">​</a></h1><p><code>noorm db create</code> provisions the database named in a stored config. It has no <code>--name</code>, <code>--dialect</code>, or <code>--host</code> flag — by design.</p><p>The reasoning is short: a noorm config is the single source of truth for <em>which</em> database, <em>where</em>, <em>as whom</em>, and <em>with what guardrails</em>. Inline flags would duplicate that record and create two ways to describe the same target. The CLI sidesteps that by requiring you to declare the config first, then create against it.</p><p>This page covers the intended order of operations and the cross-tool discovery hints that get users back on track when their muscle memory expects a <code>--name</code> flag.</p><h2 id="the-configs-first-workflow" tabindex="-1">The configs-first workflow <a class="header-anchor" href="#the-configs-first-workflow" aria-label="Permalink to “The configs-first workflow”">​</a></h2><p>End-to-end, from a clean repo to a usable database:</p><pre><code># 1. Scaffold the project (settings.yml, .noorm/, sql/, changes/).
noorm init --yes

# 2. Add one config per environment, non-interactively.
noorm config import dev-config.json
noorm config import test-config.json

# 3. Create the underlying database for each config.
noorm db create -c dev
noorm db create -c test

# 4. Pick the active config for ad-hoc commands.
noorm config use dev
</code></pre><p>After step 4, every <code>noorm</code> command that doesn&#39;t pass <code>-c &lt;name&gt;</code> inherits the active config. <code>noorm run build</code>, <code>noorm change ff</code>, and <code>noorm sql query &quot;…&quot;</code> all target the <code>dev</code> database until you switch.</p><h2 id="what-noorm-db-create-does-and-what-it-needs" tabindex="-1">What <code>noorm db create</code> does (and what it needs) <a class="header-anchor" href="#what-noorm-db-create-does-and-what-it-needs" aria-label="Permalink to “What noorm db create does (and what it needs)”">​</a></h2><p>The command connects to the dialect&#39;s <em>system database</em> (<code>postgres</code> for PostgreSQL, <code>master</code> for MSSQL; MySQL connects with no database named at all) with the credentials in the named config, then runs the dialect&#39;s <code>CREATE DATABASE</code> statement targeted at <code>config.connection.database</code>. SQLite has no system database to borrow, so noorm checks that the directory holding the file is writable instead. That probe leaves nothing behind.</p><p>Creating the database is only half the job. <code>db create</code> then connects to the new database and bootstraps noorm&#39;s own tracking tables (<code>version</code>, <code>change</code>, <code>executions</code>, <code>lock</code>, <code>identities</code>, <code>vault</code>), so the config is ready for <code>run build</code> and <code>change ff</code> when the command returns. Running it against a database that already exists and is already initialized is a no-op that reports <code>alreadyExists: true</code>.</p><p>It needs:</p><ul><li>An active config (or an explicit <code>-c &lt;name&gt;</code>).</li><li>Credentials in the config that can reach the server.</li><li>A server-level permission to create databases (e.g. <code>CREATEDB</code> on PostgreSQL, <code>dbcreator</code> role on MSSQL).</li><li>An access role that permits <code>db:create</code>. A <code>viewer</code> config is denied outright; an <code>operator</code> config needs <code>--yes</code> (or <code>NOORM_YES=1</code>) to satisfy the confirmation; an <code>admin</code> config runs unconfirmed.</li></ul><p>It does not need:</p><ul><li>The target database to already exist — that&#39;s the whole point.</li><li>A <code>.noorm/</code> schema to be built — <code>noorm run build</code> is a separate step.</li></ul><p>The access check runs before any probe touches the server. That ordering matters on SQLite, where merely opening the target creates the file: checking afterwards would hand a denied role a database anyway.</p><h2 id="why-no-name-database-flag" tabindex="-1">Why no <code>--name &lt;database&gt;</code> flag? <a class="header-anchor" href="#why-no-name-database-flag" aria-label="Permalink to “Why no --name &lt;database&gt; flag?”">​</a></h2><p>Three reasons, in order of importance:</p><ol><li><p><strong>Configs already hold the database name.</strong> The connection block in a noorm config carries <code>dialect</code>, <code>host</code>, <code>port</code>, <code>database</code>, <code>user</code>, <code>password</code>, and a handful of safety flags (<code>isTest</code>, <code>access</code>). Passing <code>--name foo</code> would let you bypass the config&#39;s <code>access</code> role and create a database the active config doesn&#39;t know about — a foot-gun.</p></li><li><p><strong>The config is also where stage defaults, secrets, and identity bindings live.</strong> A flag-only path would need its own copy of every one of those, or it would inherit silently from the active config and surprise the user.</p></li><li><p><strong>It pushes you to write the config down.</strong> <code>noorm config import</code> is non-interactive and idempotent; the JSON file becomes the reviewable artifact in your repo or CI runner.</p></li></ol><p>If you genuinely need ad-hoc database creation without a stored config, fall back to a one-shot SQL statement issued through an existing one:</p><pre><code>noorm sql query -c dev &quot;CREATE DATABASE foo&quot;
</code></pre><p>That forces you to pick a config, and therefore a connection identity, for the operation. Two caveats: the statement runs against the <code>dev</code> database rather than the system database, so <code>dev</code> itself has to exist and be reachable; and <code>CREATE DATABASE</code> classifies as <code>sql:ddl</code>, which only an <code>admin</code>-role config may run. The SQL is passed through verbatim, so it is on you to write it for the right dialect.</p><h2 id="companion-commands" tabindex="-1">Companion commands <a class="header-anchor" href="#companion-commands" aria-label="Permalink to “Companion commands”">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>Purpose</th></tr></thead><tbody><tr><td><code>noorm config import &lt;path&gt;</code></td><td>Add a config from a JSON file (non-interactive).</td></tr><tr><td><code>noorm config list</code></td><td>See which configs exist and which is active.</td></tr><tr><td><code>noorm config use &lt;name&gt;</code></td><td>Switch the active config.</td></tr><tr><td><code>noorm db create [-c &lt;name&gt;]</code></td><td>Create the database described by the named config.</td></tr><tr><td><code>noorm db drop [-c &lt;name&gt;]</code></td><td>Drop the database described by the named config.</td></tr></tbody></table><p><code>db drop</code> is gated harder than <code>db create</code>. Its <code>db:destroy</code> permission denies both <code>viewer</code> and <code>operator</code>, and an <code>admin</code> config still has to confirm, so dropping a database always takes <code>--yes</code> (or <code>NOORM_YES=1</code>). A config using the default access role is <code>user: admin</code>, which is why <code>db create</code> needs no flag on a fresh project.</p><p>For interactive setup (when you&#39;re at a terminal), <code>noorm ui</code> launches a wizard that walks through identity → config → DB create in order. Scripted setups should stick with the <code>import</code> + <code>create</code> flow above.</p><h2 id="ci-non-interactive-bootstrap" tabindex="-1">CI / non-interactive bootstrap <a class="header-anchor" href="#ci-non-interactive-bootstrap" aria-label="Permalink to “CI / non-interactive bootstrap”">​</a></h2><p>The same flow inside a CI runner — every step has a non-interactive path:</p><pre><code>#!/usr/bin/env bash
set -euo pipefail

# 1. Identity (once per runner).
noorm identity init --name &quot;$CI_USER&quot; --email &quot;$CI_EMAIL&quot;

# 2. Project scaffold.
noorm init --yes

# 3. Configs from version-controlled JSON.
noorm config import ./ci/dev.config.json
noorm config import ./ci/test.config.json

# 4. Databases.
noorm db create -c dev
noorm db create -c test

# 5. Schema + changes.
noorm config use dev
noorm run build
noorm change ff
</code></pre><p>See <a href="./../automation/non-interactive.html">Non-interactive operation</a> for the full matrix of <code>--yes</code> / <code>NOORM_YES</code> behavior.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to “Related”">​</a></h2><ul><li><a href="./../../cli/init.html"><code>noorm init</code></a> — project scaffold and identity bootstrap.</li><li><a href="./../automation/non-interactive.html">Non-interactive operation</a> — <code>--yes</code> opt-in and CI flow.</li><li><a href="./../troubleshooting.html">Troubleshooting</a> — what to do when <code>noorm db create --name foo</code> doesn&#39;t behave as expected.</li></ul>`,32)])])}const g=o(d,[["render",c]]);export{f as __pageData,g as default};
