import{_ as t,o as a,c as s,a6 as i}from"./chunks/framework.ZMCvXf1_.js";const g=JSON.parse(`{"title":"Reverse-engineering an existing database","description":"Point an agent at a database you already run and have it extract an ignatius model, using noorm's MCP server for read-only schema access.","frontmatter":{"title":"Reverse-engineering an existing database","description":"Point an agent at a database you already run and have it extract an ignatius model, using noorm's MCP server for read-only schema access."},"headers":[],"relativePath":"modeling/reverse-engineering.md","filePath":"modeling/reverse-engineering.md"}`),n={name:"modeling/reverse-engineering.md"};function o(r,e,h,d,l,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="reverse-engineering-an-existing-database" tabindex="-1">Reverse-engineering an existing database <a class="header-anchor" href="#reverse-engineering-an-existing-database" aria-label="Permalink to “Reverse-engineering an existing database”">​</a></h1><p>Most databases worth modeling already exist. The schema is in production, the reasoning behind it left with someone two jobs ago, and the only description of it is the DDL.</p><p>You do not have to model that by hand. noorm exposes the schema to a coding agent over MCP, and the <a href="/modeling/modeling-skill.html"><code>ignatius-modeling</code> skill</a> knows how to turn what it reads into entity and flow files. Your job is to set up the access, keep it read-only, and review what comes back.</p><p>The result is an ignatius model of the system you already run: a diagram to hand to people, a dictionary of what every table is for, and a base to plan the next change against.</p><h2 id="where-this-sits-in-idef1x" tabindex="-1">Where this sits in IDEF1X <a class="header-anchor" href="#where-this-sits-in-idef1x" aria-label="Permalink to “Where this sits in IDEF1X”">​</a></h2><p>Extraction is not a detour around the methodology. It is a recognized path inside it. IDEF1X calls this the <strong>bottom-up</strong> approach, inferring a logical model from the physical artifacts that already exist, and the full round trip of reverse-engineer, adjust for new requirements, then forward-engineer is <strong>re-engineering</strong>.</p><p>The discipline is also specific about how far bottom-up gets you, which is worth knowing before you start rather than after. A <em>data model</em> describes a physical database. An <em>information model</em> describes the logical requirements a system satisfies. Reverse-engineering climbs from the first toward the second in levels:</p><table tabindex="0"><thead><tr><th>Level</th><th>What you get</th><th>What it is worth</th></tr></thead><tbody><tr><td>1</td><td>Current-system documentation, a physical model</td><td>Guaranteed only to show what <strong>is</strong></td></tr><tr><td>2</td><td>Application-level logical model</td><td>The requirements the system currently satisfies, with no claim about whether they are right</td></tr><tr><td>3</td><td>Business-level model</td><td>A basis for future design, and unreachable without the business participating directly</td></tr></tbody></table><p>An agent with schema access reaches level 1 quickly and gets a good way into level 2. It cannot reach level 3 on its own, because the information at that level was never in the database. It is in the heads of the people who run the business, and the only way out is to ask them.</p><p>The IDEF1X literature is blunt about the risk, and the warning is the reason this page ends with a review step rather than a success message: <strong>a model of what you have is not a model of what you want.</strong> Assumptions compound as you climb the levels, and their source is the past, mistakes included. That is also why the skill records anti-patterns instead of quietly correcting them. Faithful first, better second, with the &quot;better&quot; being a conversation you have afterward rather than something extraction decides for you.</p><p>The <a href="https://www.skills.sh/damusix/skills/idef1x" target="_blank" rel="noreferrer"><code>idef1x</code> skill</a> carries the methodology itself if you want an agent applying it alongside you.</p><h2 id="what-you-need" tabindex="-1">What you need <a class="header-anchor" href="#what-you-need" aria-label="Permalink to “What you need”">​</a></h2><ul><li>noorm installed, with a config that connects to the database (<a href="/getting-started/installation.html">installation</a>)</li><li>ignatius installed (<a href="/modeling/installation.html">installation</a>)</li><li>The <code>ignatius-modeling</code> skill, and optionally the noorm skill for SDK and CLI conventions</li><li>An agent harness that speaks MCP, such as Claude Code or Cursor</li></ul><h2 id="_1-create-the-modeling-space" tabindex="-1">1. Create the modeling space <a class="header-anchor" href="#_1-create-the-modeling-space" aria-label="Permalink to “1. Create the modeling space”">​</a></h2><p>A model root is any folder with an <code>ignatius.yml</code> in it. Let the skill scaffold one:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/ignatius-modeling</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model</span></span></code></pre></div><p>That writes <code>ignatius.yml</code>, the group files, and the directories. To do it by hand, a one-line <code>ignatius.yml</code> is a valid model root:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> models/legacy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;name: Legacy Schema&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> models/legacy/ignatius.yml</span></span></code></pre></div><p>Put it in the repository whose schema it describes, so the model and the code stay in one history.</p><p>The rest of this page refers to that folder as <code>models/legacy</code>. Substitute wherever yours actually landed, or leave the path off entirely: <code>serve</code>, <code>export</code>, and <code>validate</code> all take the model root as an optional argument and find it by searching up and down from the current directory when you omit it.</p><h2 id="_2-connect-noorm-over-mcp" tabindex="-1">2. Connect noorm over MCP <a class="header-anchor" href="#_2-connect-noorm-over-mcp" aria-label="Permalink to “2. Connect noorm over MCP”">​</a></h2><p>noorm runs an MCP server over stdio. Generate the agent&#39;s config:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">noorm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                  # Claude Code, writes .mcp.json</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">noorm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cursor</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # Cursor, writes .cursor/mcp.json</span></span></code></pre></div><p>The agent picks up the server on next launch and gets two tools: <code>noorm_help</code> to list commands and <code>run_noorm_cmd</code> to execute them. The schema explorer, the SQL runner, and config management all arrive through those.</p><p>Make sure the config you want the agent reading is the active one, and that it points at the database you mean. Reverse-engineering staging when you meant production produces a confident model of the wrong thing.</p><h2 id="_3-keep-the-config-read-only" tabindex="-1">3. Keep the config read-only <a class="header-anchor" href="#_3-keep-the-config-read-only" aria-label="Permalink to “3. Keep the config read-only”">​</a></h2><p>Every config carries a role per channel, <code>access: { user, agent }</code>, and the <code>agent</code> role governs what a coding agent can do regardless of whether it comes in over MCP or shells out to the <code>noorm</code> binary. noorm resolves the channel from the harness that spawned it, so an agent refused over MCP cannot route around the refusal on the command line.</p><table tabindex="0"><thead><tr><th>Role</th><th>What the agent can do</th></tr></thead><tbody><tr><td><code>viewer</code></td><td>Explore schema, run read-only SQL (<code>SELECT</code>, <code>SHOW</code>, <code>DESCRIBE</code>). <code>EXPLAIN</code> inherits the class of the statement it wraps, so <code>EXPLAIN ANALYZE DELETE ...</code> is a write and is denied</td></tr><tr><td><code>operator</code></td><td>Adds <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code>; destructive commands stay out of reach</td></tr><tr><td><code>admin</code></td><td>Adds DDL, change and run commands, and vault writes. The irreversible operations (drop, teardown, truncate, config rewrite, vault propagate, force-unlock) are confirmation-gated, and a confirmation collapses to a denial on the agent channel</td></tr><tr><td><code>false</code></td><td>The config is invisible on that channel</td></tr></tbody></table><p><code>viewer</code> is all reverse-engineering needs, and it is already the default: a config that never declared <code>access</code> gets <code>{ user: &#39;admin&#39;, agent: &#39;viewer&#39; }</code>. So this step is usually a check rather than a change. Confirm it in <code>noorm ui</code> under Config → Edit, and lower it back to <code>viewer</code> if someone raised it for other work.</p><p>Raw SQL is classified by what the statement does rather than which command asked, so an agent on <code>viewer</code> gets a <code>SELECT</code> through and a same-shaped <code>INSERT</code> denied.</p><div class="warning custom-block"><p class="custom-block-title">Roles prevent accidents, not attacks</p><p>The <code>agent</code> role is a guardrail against a mistake, not a security boundary. It stops an agent from doing damage it never meant to do, which is the realistic failure. It does not stop one that has been talked into it: <code>NOORM_CHANNEL</code> is an environment variable, so anything able to influence the agent&#39;s instructions or its environment can reach around the role.</p><p>That is worth weighing here specifically, because reverse-engineering points an agent at a large amount of text you did not write. Table comments, column descriptions, and stored procedure bodies are all content a prompt injection can ride in on.</p><p>So for a database you care about, connect noorm with a database user that holds only <code>SELECT</code> and catalog permissions. Then the role is the second line of defense rather than the only one, and an instruction that gets past it still cannot write anything.</p></div><h2 id="_4-give-the-database-user-catalog-access" tabindex="-1">4. Give the database user catalog access <a class="header-anchor" href="#_4-give-the-database-user-catalog-access" aria-label="Permalink to “4. Give the database user catalog access”">​</a></h2><p>The explorer reads your database&#39;s system catalogs. Which ones depends on the dialect:</p><table tabindex="0"><thead><tr><th>Dialect</th><th>Catalog noorm reads</th></tr></thead><tbody><tr><td>PostgreSQL</td><td><code>information_schema</code> views (<code>columns</code>, <code>table_constraints</code>, <code>key_column_usage</code>, <code>referential_constraints</code>, <code>parameters</code>)</td></tr><tr><td>MySQL</td><td><code>information_schema</code> views (<code>columns</code>, <code>key_column_usage</code>, <code>referential_constraints</code>, <code>routines</code>, <code>parameters</code>)</td></tr><tr><td>SQL Server</td><td><code>sys</code> catalog views (<code>sys.columns</code>, <code>sys.foreign_keys</code>, <code>sys.foreign_key_columns</code>, <code>sys.index_columns</code>)</td></tr><tr><td>SQLite</td><td><code>sqlite_master</code> and <code>PRAGMA</code> (file access, no grants)</td></tr></tbody></table><p>This is the step that quietly ruins a model, because a permissions gap does not raise an error. It returns fewer rows.</p><p>PostgreSQL filters the information schema by privilege: &quot;Only those tables and views are shown that the current user has access to (by way of being the owner or having some privilege).&quot; SQL Server behaves the same way through metadata visibility, where &quot;the visibility of metadata is limited to securables that a user either owns or on which the user has been granted some permission,&quot; and a user with no permission on a table gets an empty result set rather than a denial.</p><p>An agent reading through an under-privileged user therefore produces a model that looks finished and is silently missing tables. Grant catalog access before you start:</p><div class="language-sql"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- PostgreSQL: visibility follows privilege</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRANT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> USAGE </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SCHEMA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> public </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> modeler;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRANT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ALL TABLES </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SCHEMA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> public </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> modeler;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- MySQL: same idea, per schema</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRANT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mydb.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TO</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;modeler&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;%&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- SQL Server: metadata visibility without data access</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRANT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VIEW </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DEFINITION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> modeler;</span></span></code></pre></div><p>Then check the count. Ask the agent, or run it yourself, for the table list noorm sees and compare it against what you know is there. A mismatch here is cheaper to catch now than after the model is written.</p><h2 id="_5-ask-the-agent-to-reverse-engineer" tabindex="-1">5. Ask the agent to reverse-engineer <a class="header-anchor" href="#_5-ask-the-agent-to-reverse-engineer" aria-label="Permalink to “5. Ask the agent to reverse-engineer”">​</a></h2><p>Launch the harness and point it at both the database and the model root:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>/ignatius-modeling discover</span></span></code></pre></div><p><code>discover</code> interviews you when nothing exists yet. When a real system is available it reads that instead, and interviews you only about the judgment calls the schema cannot answer. Tell it where the model root is and that noorm&#39;s MCP server is connected.</p><p>Code is worth offering alongside the schema. Foreign keys give the agent your entities and relationships, but only the handlers, jobs, and stored procedures that touch the data can give it the processes for the <a href="/modeling/data-flows.html">data flow diagrams</a>. A <code>SELECT</code> inside a request handler is an input flow with a column list; an <code>INSERT</code> is an output flow. Point the agent at the repository and the flows come out at column level rather than as guesses.</p><h2 id="_6-what-a-good-extraction-does" tabindex="-1">6. What a good extraction does <a class="header-anchor" href="#_6-what-a-good-extraction-does" aria-label="Permalink to “6. What a good extraction does”">​</a></h2><p>The skill&#39;s method is worth knowing, because it is what you review against.</p><p><strong>It reads rather than invents.</strong> Entities come from tables, attributes from columns with nullability preserved, relationships from foreign-key constraints, and subtype clusters from tables whose whole primary key equals their parent&#39;s. Whether an edge is identifying is derived from the key shape, never declared.</p><p><strong>It keeps the source&#39;s names.</strong> <code>sales_orders</code> stays <code>sales_orders</code>. The entity id is what every relationship target, wiki link, and <code>db:</code> token has to match, so renaming during extraction breaks the correspondence with the real system. A rename is a decision to surface, not a cleanup to perform quietly.</p><p><strong>It detects your key convention instead of imposing one.</strong> A composite primary key containing a foreign key reads as <code>key-inherited</code>; a lone surrogate <code>id</code> with foreign keys outside the key reads as <code>orm-oriented</code>. Mixed schemas stay mixed. See <a href="/modeling/entities.html#the-two-key-conventions">the two key conventions</a>.</p><p><strong>It records anti-patterns rather than fixing them.</strong> Faithful first, better second. A junk-drawer table or a surrogate key where the real identity is composite gets captured as it is, then raised with you as a question. Silent improvement during extraction produces a model of a database you do not have.</p><p><strong>It grounds the model in real rows.</strong> Sample data from the live system makes the best examples, which is why every entity and process should carry some. Mask anything sensitive before it lands in a file you commit.</p><h2 id="_7-verify-and-review" tabindex="-1">7. Verify and review <a class="header-anchor" href="#_7-verify-and-review" aria-label="Permalink to “7. Verify and review”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ignatius</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> validate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> models/legacy</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # or just \`ignatius validate\` from inside the project</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ignatius</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> models/legacy</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span></span></code></pre></div><p><code>validate</code> exits <code>1</code> on errors and <code>0</code> otherwise, so it works as a gate. A clean run means the model is internally consistent. It does not mean the model is right, and the two failures worth hunting are ones no linter catches: a table the agent could not see, and a purpose it invented because the schema could not tell it one.</p><p>Read the entity bodies with that in mind. Any entity whose description restates its columns rather than explaining what it is for is a question the agent could not answer from the schema, and the answer is in your head or nobody&#39;s. Those paragraphs are the part of the model worth the most later, and the part only you can supply.</p><p>This is the climb from level 2 to level 3, and it is the work reverse-engineering cannot do for you. An extracted model tells you what the database is. Turning that into what the business means, and then into what it should be, takes the people who run it. Serve the model, walk them through it, and let the arguments start.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to “Related”">​</a></h2><ul><li><a href="/modeling/entities.html">Entities and key inheritance</a></li><li><a href="/modeling/data-flows.html">Data flows</a></li><li><a href="/modeling/modeling-skill.html">The modeling skill</a></li><li><a href="/guide/automation/mcp.html">MCP (AI agent integration)</a> for the server itself</li><li><a href="/guide/environments/configs.html#access-roles">Access roles</a> for channel detection and the <code>NOORM_CHANNEL</code> override</li></ul>`,58)])])}const k=t(n,[["render",o]]);export{g as __pageData,k as default};
