import{_ as o,o as n,c as i,j as e,a,aa as r,ab as s,a6 as d}from"./chunks/framework.ZMCvXf1_.js";const w=JSON.parse('{"title":"Information modeling","description":"Plan the data model before you build it. ignatius turns a folder of markdown into an IDEF1X entity diagram, a searchable data dictionary, and SSADM data flow diagrams.","frontmatter":{"title":"Information modeling","description":"Plan the data model before you build it. ignatius turns a folder of markdown into an IDEF1X entity diagram, a searchable data dictionary, and SSADM data flow diagrams."},"headers":[],"relativePath":"modeling/index.md","filePath":"modeling/index.md"}'),h={name:"modeling/index.md"};function l(c,t,m,g,p,u){return n(),i("div",null,[...t[0]||(t[0]=[e("h1",{id:"information-modeling",tabindex:"-1"},[a("Information modeling "),e("a",{class:"header-anchor",href:"#information-modeling","aria-label":"Permalink to “Information modeling”"},"​")],-1),e("p",null,"Most schemas get designed while they are being built. A table appears because a feature needed it, a column gets added because a bug needed it, and the shape of the whole thing only exists in the migration history and in the head of whoever wrote it. By the time anyone can see the model, it is already in production and expensive to change.",-1),e("p",null,[e("a",{href:"https://github.com/noormdev/ignatius",target:"_blank",rel:"noreferrer"},"ignatius"),a(" moves that work earlier. It is a planning tool for information modeling: you describe entities, keys, and relationships in markdown files, and it renders them as an interactive diagram you can hand to another person. The binary never connects to a database and never generates DDL. Its output is a picture of what you intend to build, precise enough to argue about before anyone writes SQL.")],-1),e("div",{class:"demo-video"},[e("video",{src:s,poster:r,width:"1316",height:"858",controls:"",muted:"",playsinline:"",preload:"none"})],-1),d('<p>Above: a model of an LLM memory database, walked through all three views. The entity dialog, the searchable dictionary with its key-inheritance lineage, and a decomposed process flow where an external LLM agent creates and files artifacts.</p><h2 id="what-it-produces" tabindex="-1">What it produces <a class="header-anchor" href="#what-it-produces" aria-label="Permalink to “What it produces”">​</a></h2><p>One folder of markdown files drives three views of the same model.</p><table tabindex="0"><thead><tr><th>View</th><th>What it shows</th></tr></thead><tbody><tr><td>Data Graph</td><td>An IDEF1X entity-relationship diagram in crow&#39;s-foot notation. Pan, zoom, click an entity to open its full definition.</td></tr><tr><td>Data Dictionary</td><td>Every entity, process, external, and store in one searchable reference page.</td></tr><tr><td>Data Flows</td><td>SSADM data flow diagrams showing how processes move data between people, the database, and everything else.</td></tr></tbody></table><p>The diagrams are generated, never drawn. You write the structure as text, and ignatius derives the cardinality, the entity classification, and the subtype clusters from the key shape you described. Change a foreign key and the notation updates on the next reload. A model that lives in markdown diffs like code, reviews like code, and cannot drift from a picture somebody drew once in a diagramming app.</p><h2 id="why-a-web-instead-of-a-list" tabindex="-1">Why a web instead of a list <a class="header-anchor" href="#why-a-web-instead-of-a-list" aria-label="Permalink to “Why a web instead of a list”">​</a></h2><p>A table list tells you what exists. It does not tell you how anything connects, or why any of it is there.</p><p>The graph gives you the web: which entities hang off which, where identity comes from, which clusters are subtypes of a common basetype. Each entity carries a prose body explaining what it is and what business function it serves, so the answer to &quot;why does this table exist&quot; lives next to the table. Each entity also carries example rows. Two or three realistic instances make the rules concrete, and a sample row that violates a constraint you believed in reveals a modeling error no structural check will catch.</p><p>You make better decisions with a full view of the thing you are about to build. That is the whole argument for doing this before the schema exists rather than after.</p><h2 id="written-for-people-and-for-models" tabindex="-1">Written for people and for models <a class="header-anchor" href="#written-for-people-and-for-models" aria-label="Permalink to “Written for people and for models”">​</a></h2><p>The source is markdown with YAML frontmatter, which suits both readers.</p><p>A stakeholder gets the rendered app: a diagram they can explore, a dictionary they can search, and prose that explains the business meaning rather than the storage layout. <code>ignatius export</code> writes all three views into one self-contained HTML file with no external dependencies, so a reviewer opens it from <code>file://</code> and needs nothing installed.</p><p>A coding agent gets plain text it can read and write directly. ignatius comes with the <a href="/modeling/modeling-skill.html"><code>ignatius-modeling</code> skill</a> for Claude Code, which authors entity and flow files through guided Q&amp;A and verifies each one with <code>ignatius validate</code> before moving on.</p><h2 id="the-workflow" tabindex="-1">The workflow <a class="header-anchor" href="#the-workflow" aria-label="Permalink to “The workflow”">​</a></h2><p>Model first, get agreement, then build.</p><ol><li><strong>Model.</strong> Describe the entities and the processes in markdown, either by hand or through the modeling skill.</li><li><strong>Review.</strong> Serve the model or export it to HTML and walk stakeholders through the graph and the flows. Arguments about the shape of the data are cheap here and expensive later.</li><li><strong>Build.</strong> Once the model is agreed, hand it to your agent as the specification. The files that describe the model are the same files the LLM reads to write the schema.</li></ol><p>noorm is the other half of that last step. ignatius describes what you intend to build. noorm builds it, versions it, and keeps every environment in sync. The two share a bias: your schema belongs in files you own, not in an object model a tool derived for you.</p><h2 id="why-a-separate-tool" tabindex="-1">Why a separate tool <a class="header-anchor" href="#why-a-separate-tool" aria-label="Permalink to “Why a separate tool”">​</a></h2><p>noorm already explores schemas and runs SQL, so it is fair to ask why modeling is a second binary instead of a <code>noorm model</code> subcommand. Three reasons, and they all come back to ignatius being useful at a moment when noorm is not.</p><p><strong>It never touches a database.</strong> ignatius carries no driver and emits no DDL. Its value is being usable before the database exists, which is exactly the moment a tool that requires a connection has nothing to offer. Putting the planning step inside noorm would gate it behind the thing you have not built yet.</p><p><strong>It does not need noorm.</strong> Nothing in the format is noorm-specific: markdown in, IDEF1X and SSADM diagrams out, with no dialect anywhere in the pipeline. A team on Prisma, ActiveRecord, or hand-written migrations gets the same thing from it. Making it a subcommand would shrink its audience to noorm users and buy nothing.</p><p><strong>The audiences differ.</strong> <code>ignatius export</code> produces a file for the person approving a design, not the person deploying a schema. That reader is not going to install a schema and change manager to look at a diagram.</p><p>There is a practical consequence worth knowing as you read: the two ship on their own cadences from their own repositories. That is why these pages carry the narrative and link out to the ignatius repo for the exhaustive rule catalogs rather than copying them here, where they would drift out of date.</p><h2 id="two-notations-two-questions" tabindex="-1">Two notations, two questions <a class="header-anchor" href="#two-notations-two-questions" aria-label="Permalink to “Two notations, two questions”">​</a></h2><p>ignatius answers a different question in each of its diagram views.</p><p><strong>IDEF1X</strong> answers &quot;what is the data, and how is it identified&quot;. It is the premise the entity side is built on, which is why key inheritance gets first-class treatment: identifying relationships, compound keys that carry their lineage, and subtype clusters with exclusive or inclusive discriminators. See <a href="/modeling/entities.html">Entities and key inheritance</a>.</p><p><strong>SSADM data flow diagrams</strong> answer &quot;who touches the data, and where does it live&quot;. Processes, external actors, and data stores. Stores are not only tables. A cache, a log file, a queue, a paper form, and a document all appear as first-class stores, so the diagram shows where data rests rather than only the part of it that reached a database. See <a href="/modeling/data-flows.html">Data flows</a>.</p><h2 id="where-the-name-comes-from" tabindex="-1">Where the name comes from <a class="header-anchor" href="#where-the-name-comes-from" aria-label="Permalink to “Where the name comes from”">​</a></h2><p>ignatius is named for Derek Ignatius Asirvadem, who mentored me for a couple of years and introduced me to IDEF1X.</p><p>Studying it and then practising it convinced me there is no higher standard for modeling data. The industry mostly skips modeling and goes straight to building. Modeling first solves something like 80% of the problems up front, and it produces months of valid programming work before anyone opens an editor.</p><p>The other 20% is real and worth naming: once you start building, you <strong>do</strong> find problems in the model. That is the part that drifts. What matters is when you find it. While nothing is physically committed to the system yet, changing the model is cheap. Once it is code, it is not. Modeling holds that cheap window open longer, and an agent working from a written model holds it open longer still.</p><p>Before ignatius I drew models in OmniGraffle, on Derek&#39;s recommendation. Two things broke that. My own obsession with perfect lines and perfect names turned drawing into the bottleneck, and there was no way to tell an LLM &quot;this is the relationship between these two entities&quot; from a picture. So the model moved into markdown, where both problems go away. The layout is generated rather than drawn, and the source is text an agent can read and write.</p><p>None of these tools would exist without his guidance. ignatius is a tribute to him.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next steps”">​</a></h2><div class="next-steps"><p><a href="/modeling/installation.html"><strong>Installation</strong></a> Install the binary and the modeling skill.</p><p><a href="/modeling/entities.html"><strong>Entities and key inheritance</strong></a> The entity format, what gets derived, and the two key conventions.</p><p><a href="/modeling/data-flows.html"><strong>Data flows</strong></a> Processes, externals, stores, and sub-diagrams.</p><p><a href="/modeling/reverse-engineering.html"><strong>Reverse-engineering</strong></a> Extract a model from a database you already run.</p><p><a href="/modeling/best-practices.html"><strong>Best practices</strong></a> How to get a model worth showing to a stakeholder.</p></div>',35)])])}const y=o(h,[["render",l]]);export{w as __pageData,y as default};
