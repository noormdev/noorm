import{_ as t,o as i,c as a,a6 as o}from"./chunks/framework.ZMCvXf1_.js";const g=JSON.parse('{"title":"The modeling skill","description":"/ignatius-modeling authors ignatius entities and data flow diagrams through guided Q&A in Claude Code, and verifies each file with ignatius validate.","frontmatter":{"title":"The modeling skill","description":"/ignatius-modeling authors ignatius entities and data flow diagrams through guided Q&A in Claude Code, and verifies each file with ignatius validate."},"headers":[],"relativePath":"modeling/modeling-skill.md","filePath":"modeling/modeling-skill.md"}'),s={name:"modeling/modeling-skill.md"};function n(d,e,r,l,h,c){return i(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="the-modeling-skill" tabindex="-1">The modeling skill <a class="header-anchor" href="#the-modeling-skill" aria-label="Permalink to “The modeling skill”">​</a></h1><p><code>/ignatius-modeling</code> is a Claude Code skill that authors a model through question and answer, then checks its own work. It writes one entity, a data flow diagram, a fresh model skeleton, or a full discovery session that works the model out from how your business runs.</p><p>The skill exists because the hard part of modeling is not the YAML. It is knowing which questions to ask, in which order, and noticing when an answer contradicts an earlier one. The skill carries that interview structure, and you supply the domain knowledge.</p><p><strong>Prerequisites:</strong> Claude Code with skill support, and the <code>ignatius</code> binary on your <code>$PATH</code> or built locally with <code>bun run build:cli</code>.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to “Install”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/noormdev/ignatius</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ignatius-modeling</span></span></code></pre></div><p>That adds <code>ignatius-modeling</code> to the current project&#39;s <code>.claude/skills/</code>. Add <code>-g</code> to install it globally for every project on the machine. Reload skills in Claude Code and <code>/ignatius-modeling</code> becomes available.</p><h2 id="modes" tabindex="-1">Modes <a class="header-anchor" href="#modes" aria-label="Permalink to “Modes”">​</a></h2><table tabindex="0"><thead><tr><th>Invocation</th><th>What it does</th></tr></thead><tbody><tr><td><code>/ignatius-modeling entity</code></td><td>Interactive Q&amp;A to author one entity file</td></tr><tr><td><code>/ignatius-modeling model</code></td><td>Bootstrap a new model skeleton: <code>ignatius.yml</code>, group files, directories</td></tr><tr><td><code>/ignatius-modeling flow</code></td><td>Interactive Q&amp;A to author a <a href="/modeling/data-flows.html">data flow diagram</a></td></tr><tr><td><code>/ignatius-modeling discover</code></td><td>Socratic interview that works the model out from how your business runs, generating entities and flows</td></tr><tr><td><code>/ignatius-modeling</code></td><td>Prompts you to choose a mode</td></tr></tbody></table><h2 id="choosing-between-flow-and-discover" tabindex="-1">Choosing between flow and discover <a class="header-anchor" href="#choosing-between-flow-and-discover" aria-label="Permalink to “Choosing between flow and discover”">​</a></h2><p><code>flow</code> and <code>discover</code> are two doors into the same artifacts.</p><p>Pick <code>flow</code> when you can already name your processes. It walks the structure step by step: processes as verbs, externals, the decision between a <code>db:</code> store and a <code>kind:</code> store, the data each flow carries, sample rows, and the business narrative.</p><p>Pick <code>discover</code> when you know what the business does but have not decomposed it yet. The skill interviews you in plain language, derives the entities your processes require, writes those first, then writes the flows that reference them. When a real system already exists, whether a database, a schema dump, a codebase, or an API, <code>discover</code> reads it instead of interviewing you and then walks you through the judgment calls it cannot infer. To point it at a live database, see <a href="/modeling/reverse-engineering.html">Reverse-engineering an existing database</a>.</p><p>Both modes always produce example data. Every entity gets sample rows and every process gets input and output tables, because concrete instances expose wrong rules that pass every structural check.</p><h2 id="convention-detection" tabindex="-1">Convention detection <a class="header-anchor" href="#convention-detection" aria-label="Permalink to “Convention detection”">​</a></h2><p>The skill works out which key convention a model uses from the shape of its existing entities rather than asking you to choose a mode up front, and a model can mix both styles per entity.</p><table tabindex="0"><thead><tr><th>Convention</th><th>Primary key shape</th><th>Foreign key placement</th></tr></thead><tbody><tr><td><code>key-inherited</code></td><td>Composite: parent primary-key columns plus a local discriminator</td><td>Inside the child&#39;s primary key</td></tr><tr><td><code>orm-oriented</code></td><td>A single surrogate <code>id</code>, typically integer or uuid</td><td>Outside the primary key, as plain columns</td></tr></tbody></table><p>You never set <code>classification</code> or <code>identifying</code> by hand in either convention. The parser derives both from the key shape you describe. See <a href="/modeling/entities.html#what-gets-derived">what gets derived</a>.</p><h2 id="the-verification-loop" tabindex="-1">The verification loop <a class="header-anchor" href="#the-verification-loop" aria-label="Permalink to “The verification loop”">​</a></h2><p>After writing each file the skill runs <code>ignatius validate &lt;model-root&gt;</code> and parses the findings from stderr. Findings are reported with fix hints, and you can ask the skill to revise and re-run, up to five attempts. A clean run with no findings confirms the file is valid.</p><p>This is the part that makes agent-authored models trustworthy. The skill is not asked to be right on the first try. It is asked to converge against a linter that checks unknown targets, dangling foreign-key columns, missing discriminators, unknown columns in flow contracts, and unbalanced decompositions.</p><h2 id="from-model-to-schema" tabindex="-1">From model to schema <a class="header-anchor" href="#from-model-to-schema" aria-label="Permalink to “From model to schema”">​</a></h2><p>A validated model is a specification, and the same markdown a stakeholder reviewed is what the agent reads to build.</p><p>Once the model is agreed, point Claude Code at the model root and ask it to write the SQL. The entity files carry the keys, the columns, the relationships, and the prose explaining what each one is for, which is more context than a schema request usually gets. From there, <a href="/getting-started/installation.html">noorm</a> takes over: SQL files in directories, <code>noorm run build</code> to build a fresh database, and changes for existing ones.</p><p>The division of labor is worth stating plainly. ignatius owns the decision about what the data is. noorm owns building and versioning it. Neither tool reaches into the other&#39;s job, and both keep the source of truth in files you own.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to “Related”">​</a></h2><ul><li><a href="/modeling/entities.html">Entities and key inheritance</a> for the format the skill writes.</li><li><a href="/modeling/data-flows.html">Data flows</a> for the diagram format.</li><li><a href="/modeling/best-practices.html">Best practices</a> for how to review what the skill produces.</li></ul>',27)])])}const u=t(s,[["render",n]]);export{g as __pageData,u as default};
