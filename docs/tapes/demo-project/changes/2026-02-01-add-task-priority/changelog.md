# Add task priority

Tasks gained a 1-5 priority with a check constraint. The SQL file in
`sql/01_tables/003_task.sql` describes the column as it exists today; this
change is how databases built before it catch up.
